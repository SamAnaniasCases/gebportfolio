# Anonymous Shared Chess Game Implementation Roadmap & Checkpoints

<callout icon="♞">**Status:** Completed · **Owner:** Gen · **Date:** 2026-07-30</callout>

---

## 1. Roadmap Overview

This roadmap defines the completed implementation checkpoints for integrating `@samananias/turn-arbiter` into the portfolio application and launching the shared crowd-chess experience inside a split Chat/Chess modal.

---

## 2. Execution Phases & Checkpoints

### Phase 0: Local Package Linking & Engine Harness

- [x] Add `"@samananias/turn-arbiter": "file:../turn-arbiter"` to `package.json`.
- [x] Execute `cmd /c "npx pnpm install"` to bind the local dependency.
- [x] Run `cmd /c "npm test"` inside `C:\Users\Sam\Sam Folder\Repository\turn-arbiter` to confirm pure unit test suite passes.

### Phase 1: Storage Infrastructure & CAS Adapter

- [x] Create D1 table definitions (`game`, `finished_game`, `rate_limit`) in `src/lib/chess/db.sql`.
- [x] Implement D1 database storage provider with CAS atomic SQL query handling.
- [x] Implement local mock in-memory storage fallback for offline development.
- [x] Implement HMAC-SHA256 session cookie generator & signature validator.

### Phase 2: Astro Server API Endpoints

- [x] Create `/api/chess/state.ts` (`GET`) returning sanitized `publicView` state and setting signed session cookie ONLY when called by an onboarded chat session.
- [x] Create `/api/chess/move.ts` (`POST`) evaluating turn permissions via `@samananias/turn-arbiter` and handling CAS `superseded` races.
- [x] Wire IP rate limiter token bucket middleware.

### Phase 3: Split Chat/Chess Modal & UI Integration

- [x] Update `ChatBox.tsx` onboarding screen: username entry + "Play" / "Join Chat & Play" action button.
- [x] Gate `/api/chess/state` team assignment call behind username onboarding completion (`hasOnboarded`).
- [x] Implement split modal layout in `ChatBox.tsx`: **Chatbox feed on the left**, **Chess game board on the right**.
- [x] Integrate vector SVGs from `src/assets/chess/` (`black-*.svg`, `white-*.svg`) into piece rendering.
- [x] Create `ChessWidget.tsx` React component embedded on the right panel for interactive chess play, move drag/click, turn state updates, and race condition feedback.
- [x] Ensure passive portfolio viewers (who do not open chat and enter username) receive zero chess cookies and zero team allocation.

### Phase 4: Polish & Resilience

- [x] Implement 48-hour stall breaker state transition UI inside chat modal.
- [x] Wire game completion detection and PGN archive capture to `finished_game`.
- [x] Add atmospheric state copy ("A teammate moved first", visitor counter, team indicators).

### Phase 6: 3D Stage Scene Graph & Projection Engine Refinement

- [x] Create centralized virtual camera configuration (`CAMERA = { perspective: 1400, boardTilt: 58, pieceTilt: -46 }`) and decoupled visual style config (`PIECE_STYLES`) in `src/components/chess/sceneConfig.ts`.
- [x] Implement centralized `projectPiece(x, y, type, isSelected, camera)` projection function converting board coordinates to screen space position, depth, height, and tilt.
- [x] Decouple 8x8 Board Surface Grid from Piece Overlay Layer (board is 100% agnostic of piece children).
- [x] Implement dynamic screen-space depth sorting (`pieces.sort((a,b) => a.y - b.y)`).
- [x] Streamline 4-layer Standee structure (`Shadow` -> `Base` -> `Thickness` -> `SVG Artwork`).
- [x] Physical 3D wooden block slab extrusion (`.board-3d-slab`), ground drop shadow, and frameless 3D/2D stage rendering.
- [x] Global top-right anti-spam Toast Notification System (`Toast.tsx`) featuring `inaccuracy.svg` and duplicate suppression.
- [x] Integrated 3D View perspective toggle and borderless Details SVG button (`mistake.svg`) into Chatbox header control toolbar.

---

## 3. Definition of Done

This feature is complete: all Phase 0 through Phase 6 checkpoints pass, the full verification suite executes cleanly with zero errors, and `Changelog.md` is updated.
